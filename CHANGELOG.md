# 更新日志

所有重要更改都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

**重要说明：每次代码修改都必须将更新内容写入本文件；只有发布级或用户可见变更才提升版本号。**

## [3.0.0] - 2026-08-28

### 新增
- 新增 PostgreSQL 18 原生 schema、版本表与 advisory lock 启动迁移，正式支持 PostgreSQL identity、事务、`ON CONFLICT` 和 Psycopg 3 字典行。
- 新增飞牛 OS 私有单 YAML 生成器，在不输出秘密的前提下生成独立的数据库管理员、应用数据库、Web 登录密码及稳定 `IPTV_SECRET_KEY`。
- 新增 `MIGRATING-3.0.md`，记录 MySQL 备份边界、PostgreSQL 恢复验收、回滚和后续 major 升级要求。
- 在迁移验收中明确 MySQL `utf8mb4_unicode_ci` 与 PostgreSQL `C.UTF-8` 的大小写、重音和排序差异，要求恢复前检测并人工确认等价值冲突。
- 新增 PostgreSQL 18 真实集成测试，覆盖幂等建库、事务回滚、级联删除、UPSERT、identity、4096 字符 URL、搜索/排序、小数统计、并发租约和断线重连。

### 改进
- 运行时数据库从 MySQL 8.4 完整切换到 PostgreSQL 18，应用连接默认改为 `postgres:5432/iptv_all_in_one`，并保持现有 HTTP API 与 `bandwidth_MBps` JSON 字段兼容。
- 将 MySQL DDL、元数据查询、UPSERT、分页、搜索和排序统一改为 PostgreSQL 语义；统计除法显式使用浮点，并固定 NULL 排序。
- URL 唯一与复合索引改用 `pgcrypto` SHA-256 表达式，避免最长 4096 字符 URL 超出 PostgreSQL B-tree 索引项上限。
- URL 精确查询同时匹配 SHA-256 与原值，使 PostgreSQL 可使用表达式索引并继续防御摘要碰撞。
- 来源汇总、总览关联与优先级更新同样通过 URL 摘要索引定位并复核原值，避免百万级持久化结果表上的重复全表扫描。
- Docker 镜像发布扩展为 `linux/amd64` 和 `linux/arm64`，README 与 Docker Hub 文档改为 PostgreSQL 3.0 单 YAML 流程。
- 同步前端包版本为 3.0.0，并更新数据库元数据与部署安全回归测试。
- Compose smoke test 直接校验空库匿名订阅的 M3U 响应，避免把下载接口正常的无数据 404 误判为部署失败。
- 修正前端 CI 的 `setup-node` 固定提交 SHA，并补充 PostgreSQL 逻辑备份、隔离恢复演练、RPO/RTO 与私有密钥联动说明。

### 修复
- 将任务租约获取改为 PostgreSQL 原子条件 UPSERT，修复目标行不存在时并发请求可能同时取得租约的问题。
- 修复 PostgreSQL 整数除法会把总览通过率截断为 0/1，以及默认 NULL 排序差异可能改变分页和质量排序的问题。
- 修复 PostgreSQL 对 `ROUND(double precision, digits)` 不支持导致来源分组统计失败，以及数据库断线时日志失败批次重复获取非重入锁造成死锁的问题。
- 收窄断线恢复为执行前安全重连，禁止在结果未知时透明重放写语句；为扫描、检测、URL UPSERT 与清理批次补齐显式事务，避免中途失败后部分提交。
- 修复 Compose 冒烟测试使用 `ci-admin`、而交付模板实际配置 `admin`，导致认证请求返回 401 并以 `curl` 退出码 22 阻断发布的问题；测试现会校验 CI 与模板账号保持一致。

### 安全
- PostgreSQL 管理员与常驻 `iptv_app` 账号分离，启用 SCRAM 认证；管理员密码不再传入应用容器。
- PostgreSQL 宿主端口仅绑定 `127.0.0.1:5432`，应用数据库流量使用隔离 Docker 内网；Web 继续以非 root、只读根文件系统和无 capabilities 运行。
- 将私有 `docker-compose.fnos.yml` 与含业务数据的 `iptv_backup.sql` 同时排除出 Git 和 Docker 构建上下文。
- 将用于 2.x 回滚的本地 `.env` 及可能包含嵌套私有 YAML 的 `deploy/` 一并排除出 Docker 构建上下文，避免远程 BuildKit 上传历史凭据。

### 运维
- 移除默认 MySQL Compose、PyMySQL 运行依赖、旧 MySQL 账号迁移入口及 `.env.example` 部署模板；3.0 不再提供 `.env` 部署流程，旧 MySQL 数据卷不能直接复用。
- PostgreSQL 18 数据卷使用 `/var/lib/postgresql`，应用端口保持 `58080`；FRP 与 MySQL 备份恢复均保持为独立运维操作，不进入应用启动链路。

## [2.1.2] - 2026-08-27

### 改进
- 统一控制台业务链路术语为“测绘采集 → 候选源池 → 全量测速 → TXT/M3U 播放列表”，并将指定目标发现和周期维护分别统一为“IP 探测”和“健康复检”，同步覆盖导航、页面、任务状态、操作提示与日志正文。
- 候选频道进入测速时的来源标签改为“候选源池 · 平台名”，展示历史记录时兼容映射旧的“扫描结果池 · 平台名”，避免升级后来源名称混用或被错误脱敏。
- 明确候选源池导出内容并非最终播放列表，相关操作统一标注为候选 M3U/CSV；增加前端文案契约及来源标签兼容测试。

### 修复
- 为测绘平台包补齐全局 `scan_state` 的兼容导出并校验对象同一性，确保遗留平台导入路径不会再次让 Quake、Hunter、DayDayMap、ZHGX、JSMpeg 等采集任务整轮跳过。

## [2.1.1] - 2026-08-27

### 修复
- 修复平台模块拆分后扫描停止状态仍从错误包层级导入，导致 Quake、Hunter、DayDayMap、ZHGX、JSMpeg 等平台在采集开始时抛出 `cannot import name 'scan_state'` 并整轮返回 0 条的问题。
- 修正 Tvheadend 和 IPTV互动专项扫描的 Hunter 单页数量上限，并将 IPTV互动标题条件改为 Hunter 的 `web.title` 字段，避免请求参数或查询字段不兼容而稳定返回 0 个候选。
- 增加平台停止状态和 Hunter 专项查询回归测试，确保包层级或平台协议调整不会再次中断采集。

## [2.1.0] - 2026-08-24

### 新增
- 数据来源质量页现在会将融合测速的扫描结果按测绘平台标记为“扫描结果池 · 平台名”，可直接区分 Quake 360、DayDayMap、Hunter 等来源；缺少平台元数据时统一显示为“扫描结果池 · 未标注平台”。

### 改进
- 扫描结果进入测速任务时保留其来源标签，并为已有历史结果按播放地址回查持久化扫描池的平台，避免再将已知扫描渠道合并为“未知来源”。
- 将来源质量页的“订阅源”文案调整为“数据来源”，准确覆盖订阅源和扫描来源两类数据。

## [2.0.6] - 2026-08-24

### 改进
- 移除配置中心里占用页面的部署安全迁移状态卡片及对应前端请求；旧凭据、root 数据库账号或未启用扫描 Key 加密时不再反复显示升级提示，原有兼容启动行为保持不变。

### 修复
- 修复扫描配置组件暴露了不存在的保存函数，导致点击“扫描配置”后运行时抛出 `ReferenceError`、页签已选中但内容区域空白的问题。
- 增加扫描配置组件挂载回归测试，覆盖初始配置与 Key 列表加载，并校验对外保存入口可用。

## [2.0.5] - 2026-08-12

### 改进
- 将 README 压缩为面向用户的快速入口，合并重复的部署、配置和排障说明，只保留核心能力、Docker 快速开始、升级入口、安全边界与开发命令；详细迁移和变量说明由专门文档承载，并同步 Docker Hub 长描述。

### 修复
- 修复 BasicAuth 临时凭据回归测试未隔离 CI 全局 `IPTV_AUTH_USERNAME`，导致运行环境设置 `ci-admin` 时错误断言默认用户名并阻断发布的问题。
- 修复 2.0 镜像无条件启用严格凭据检查，导致沿用 1.x `root` 数据库账号或缺少新增凭据的现有部署在拉取镜像后反复退出、完全无法使用的问题；旧部署现在兼容启动并在日志与配置中心显示迁移告警，即使最初 2.0 Compose 仍显式传入严格开关也不会再因凭据检查中断服务。
- 恢复 2.0 之前常驻 Web 服务的兼容启动语义：历史数据库结构、DDL 权限或 API Key 迁移异常会记录完整告警，但不再让 Gunicorn 反复退出；一次性 `migrate-2-0` 仍严格校验并在失败时拒绝激活新账号。
- 恢复未配置 BasicAuth 时在启动日志显示本次临时登录凭据，并允许缺少主密钥的旧明文扫描 API Key 保持可读、可管理；在配置稳定的 `IPTV_SECRET_KEY` 前不会写入重启后无法解密的临时密文。
- 修复旧 `.env` 的 `DB_USER=root` 被传给官方 MySQL 镜像初始化变量而触发拒绝启动，以及只读迁移容器缺少 `.env`、无法执行纯环境变量迁移的问题。
- 修复旧 `.env` 缺少 `DB_USER` 时被 2.0 默认改用 `iptv_app`、未启用 migration profile 仍因变量插值失败，以及新旧数据卷身份不一致造成无法写入的问题；镜像默认保持 UID 10001，以兼容标准 1.x 和最初 2.0 Compose，新版 Compose 可为 root-owned 自定义挂载显式进入兼容 root，并仅增加 `DAC_OVERRIDE`。
- 修正 1.x 升级文档遗漏账号激活步骤的问题；统一为 `generate_env.py --upgrade`、`migrate-2-0`、`generate_env.py --finalize-upgrade`、启动应用的四步两阶段流程。
- 修正迁移能够“全事务回滚”的不准确描述：MySQL 用户 DDL 不可事务回滚，仅对本轮新建账号在后续失败时尝试补偿删除；配置与旧 API Key 更新继续由数据库事务保护。
- 明确已有同名数据库账号不会被迁移自动改密，避免重试时意外覆盖运维预置账号。
- 增加 `generate_env.py --recover-interrupted-upgrade`，可无损恢复最初 2.0 脚本已提前切换应用账号、但专用数据库账号尚未创建的中断状态；无法可靠识别时拒绝写入并要求恢复升级前备份。
- 增加 `generate_env.py --enable-container-hardening`，在运维确认旧数据卷属主后统一启用 UID 10001、只读根文件系统和严格凭据模式，避免依赖手工编辑多个开关。

### 安全
- 1.x 升级暂存阶段保留当前活动 root 凭据，仅在专用账号通过连接验证后才允许 finalize 切换并启用严格凭据模式；迁移失败时禁止继续激活或重建 `IPTV_SECRET_KEY`。
- 新部署仍由生成器显式启用严格凭据模式；CI 同时验证严格新部署和旧版 root、短密码、无新增凭据的真实健康启动路径，防止后续安全加固再次破坏升级兼容性。

## [2.0.4] - 2026-08-11

### 改进
- 精简 README，合并重复的 Docker 部署、配置和常见问题说明，将完整版本历史与升级细节交由变更日志和迁移文档承载，保留快速部署、安全边界及常用入口。

### 修复
- 将 Docker Hub 短描述从 116 字节精简为 86 字节，修复其超过平台 100 字节上限导致描述更新返回 HTTP 400 的问题。

### 运维
- 发布工作流在调用 Docker Hub API 前同步校验短描述的 UTF-8 字节数，超限时直接输出实际值与预算。

## [2.0.3] - 2026-08-11

### 修复
- 修复 Docker Hub 完整描述拼接全部历史版本后超过约 25 KB 上限，导致镜像已推送但发布工作流最终以 HTTP 400 失败的问题。

### 运维
- Docker Hub 描述现在仅附加最新版本日志，并在提交 API 前执行 24 KB 字节预算检查；API 拒绝请求时会输出响应正文以便定位字段错误。
- Docker Hub 快速开始同步为 2.0 强凭据、专用数据库用户、只读数据卷和匿名订阅风险说明。

## [2.0.2] - 2026-08-11

### 安全
- 将 `aiohttp` 从 3.11.12 升级到 3.14.3，修复 Trivy 报告的压缩响应资源耗尽等两个高危漏洞。
- 构建运行时镜像时先升级 Debian 已提供修复的系统包，再安装 FFmpeg、curl 和时区数据。

### 运维
- Trivy 门禁改为仅扫描漏洞，并继续阻断存在可安装修复版本的 HIGH/CRITICAL；对 Debian、FFmpeg 等上游尚无修复版本的 CVE 保留为已知残余风险，不再永久阻塞 Docker Hub 发布。

## [2.0.1] - 2026-08-11

### 修复
- 修复 Docker smoke test 使用未固定属主和模式的临时文件系统覆盖 `data/`、`output/` 后，非 root 写权限检查可能失败并阻断 Docker Hub 发布的问题。

### 运维
- Docker 候选镜像现在按正式 Compose 约束使用只读根文件系统，并为 `/tmp`、`data/`、`output/` 显式设置 tmpfs 的 UID、GID、模式和容量。
- 运行时安全门禁现在输出容器用户、只读状态及受检路径权限；断言失败时会直接指出具体路径和原因，不再只显示无上下文的 `exit code 1`。

## [2.0.0] - 2026-08-08

### 新增
- 新增 SQL 聚合的 `GET /api/dashboard` 与统一任务状态 `GET /api/tasks`，首页集中展示扫描质量、订阅质量、最近趋势和活动任务摘要。
- 新增来源、扫描结果和检测结果的服务端分页、搜索、质量筛选与排序，并区分“导出当前页”和“导出全部筛选结果”。
- 新增 `generate_env.py --upgrade`、一次性 `migrate_2_0.py` 和 [MIGRATING-2.0.md](MIGRATING-2.0.md)，用于创建专用 MySQL 用户并迁移加密旧 API Key。
- 新增后端安全/任务回归、MySQL 集成测试、前端 Vitest/Vue Test Utils 测试、构建体积预算、Docker smoke test 与 Trivy 镜像门禁。

### 改进
- 管理后台重构为高信息密度质量控制台：桌面折叠分组侧栏、手机分组抽屉、Hash 路由、独立总览/任务状态、统一异步三态、可见页自适应轮询和浅色/深色主题。
- 配置中心合并扫描配置与系统配置；日志仅渲染最近 500 行，文本编辑器增加未保存提示、请求序列保护和取消旧请求能力。
- TDesign 改为构建期按需自动导入，并显式加载全局明暗主题 token；页面按路由异步加载，并对首页 gzip 与单 chunk 大小执行预算检查。
- 数据库连接默认 `autocommit=True`；删除、导入、迁移和批量更新改用显式事务，请求与后台任务退出时统一回滚未完成事务并关闭线程连接。
- 测速、频道扫描和 IP 扫描使用数据库原子租约与进程内任务控制器；触发/停止统一返回 `202` 和稳定 `task_id`，超时会取消 Future 并等待清理。

### 修复
- 修复 pending 校验遗漏 `await`，并将熔断结果记为 `skipped`，不再累计连续失败或触发自动删除。
- 修复停止握手、旧 idle 状态覆盖新任务、活动任务强制清理、后台超时后继续写库和多线程数据库连接残留问题。
- 修复 schema 元数据键名大小写差异、迁移错误被吞掉、事务内读取陈旧以及检测/扫描过滤排序与日志顺序问题。
- 修复按需组件样式未注入 TDesign 全局主题变量，导致深色状态仍显示浅色回退颜色的问题；统一补充高对比键盘焦点轮廓，并将 README 快捷键映射同步为新版导航顺序。
- 修复扫描频道分组接口失败时误显示为空数据的问题，失败状态现在持续展示错误原因与重新加载操作。
- 修复异步页面分块下载期间可能短暂空白的问题，所有路由分块现在从首帧起显示加载骨架，加载失败时显示统一错误与重试界面。
- 修复输入框或可编辑区域内的 `Alt+1–9` 等快捷键仍可能切换页面的问题，编辑期间不再拦截浏览器或表单按键。

### 安全
- Web 配置、导入和导出不再接受 `output_txt/output_m3u`；仅允许运维通过 `IPTV_OUTPUT_DIR` 选择目录，输出文件固定为 `result.txt`、`result.m3u` 和 `history.json`。
- 扫描平台 API Key 使用 `IPTV_SECRET_KEY` 加密存储；接口仅返回 HMAC `key_id` 和后六位，更新与删除不再接收或泄露旧完整 Key。
- 修改类 API 强制同源 Origin、`X-IPTV-Request: 1` 和预期 Content-Type；请求体、配置导入、远程列表、探测响应和 IP 扫描展开规模均增加硬上限。
- 新增固定 DNS/实际 peer IP 核验、逐跳重定向校验和响应大小限制的网络请求层，阻断 DNS 重绑定、回环、链路本地、组播与云元数据目标；移除 TLS 失败后的自动降级。
- 正则别名改用 50 ms 超时匹配并限制频道名长度；M3U 属性和行内容会清除控制字符并安全转义。
- 匿名 TXT/M3U 地址继续公开，但增加每 IP 60 次/分钟限流、30 秒缓存、ETag、并发生成保护和 `Retry-After`。

### 运维
- Docker 应用改为非 root、只读代码/依赖/前端产物，仅 `data/`、`output/` 和受限 tmpfs 可写；应用数据库密码与 root 密码彻底分离。
- Docker 镜像默认启用强凭据 fail-closed；常驻应用容器不再接收 MySQL root 密码，Compose 保留全网卡 `58080` 与匿名订阅的风险提示。
- 发布工作流在推送镜像前调用可复用质量门禁，并将 GitHub Actions 固定到完整 commit SHA；`sync-from-codeup.yml` 保持不变。

### 升级提示
- 2.0 包含 API、配置 schema、数据库账号和输出路径的破坏性变更；升级前必须备份 MySQL 与 `.env`，完整步骤见 [MIGRATING-2.0.md](MIGRATING-2.0.md)。

## [1.8.2] - 2026-07-29

### 新增
- 前端新增零额外依赖的布局契约测试，覆盖分组导航完整性、桌面/手机壳层、数据页局部滚动约束和全部 Vue 单文件解析，可通过 `npm test` 执行。

### 改进
- 管理后台从九项横向平铺标签重构为桌面分组侧栏与独立工作区页头，按工作台、任务、数据、配置归类功能；概览统计只在总览展示，超宽屏内容宽度与视觉层级更集中。
- 建立统一的前端布局 token、卡片、响应式工具栏和表格滚动容器，并整理历史明细、系统测试、频道扫描、IP 扫描、扫描结果、检测监控及配置页的间距、圆角、操作区和触屏复制入口。
- 手机和平板端改用可横向滚动的紧凑导航；配置字段、筛选栏、下载/订阅操作和任务按钮按窄屏自动换行或满宽排列，弹窗在手机端限制为视口内尺寸。

### 修复
- 修复历史、检测、扫描结果和 IP 扫描表格在窄屏撑开整页，以及系统配置操作按钮、扫描配置参数网格和长订阅地址在手机端被裁切的问题；数据表现在只在自身容器内横向滚动。

## [1.8.1] - 2026-07-29

### 改进
- 扫描深度检测的带宽统一采用 MB/s（MiB/s）存储、展示和配置；扫描配置页可直接设置最低带宽、最大延迟和最低稳定性，扫描结果及来源聚合表同步显示 MB/s。
- C 段扩展扫描改为单轮全局共享网段/IP 预算，并增加单来源限额、按“/24 网段 + 端口”的 TTL 缓存、种子 IP 排除和扫描收益统计，避免多平台重复探测或提前耗尽预算。
- 质量热点补源按网段轮询分配 IP:端口候选，优先覆盖更多热点段，不再让前序网段占满候选额度。
- 补齐 TXIPTV、标准直播接口、ZHGXTV、Tvheadend 四类质量画像查询，恢复质量优先发现链路。

### 修复
- 深度检测改为硬性质量准入：高稳定性不再掩盖低带宽、高延迟或低稳定性频道。
- 修复扫描平台子包对上层配置、网络和日志模块的相对导入错误，以及缺失的平台超时常量，确保真实采集与 C 段扩展能够正常启动。
- 数据库启动时会一次性将历史扫描带宽从 KiB/s 换算为 MB/s，并自动对齐 `scan_yield_stats.scan_id` 与 `scan_runs.scan_id` 的字符集/排序规则，修复统计关联的排序规则冲突。

## [1.8.0] - 2026-07-12

### 新增
- 扫描配置页新增“搜索关键词”编辑器，主扫描规则存入 MySQL `config_data.scan_config`，支持逐行添加、删除、注释、恢复默认，以及用 `&&` 表示多特征同时匹配、用 `title:` 指定标题搜索。
- 新增跨平台查询构建器，同一组数据库关键词会自动转换为 Quake、Hunter、DayDayMap 和 Fofa 各自的字段语法。

### 改进
- 每轮频道扫描开始时读取一次最新关键词配置并固定为本轮快照；保存后无需重启服务，下一轮扫描即可生效，同时避免扫描进行中途修改导致同一轮查询条件不一致。
- DayDayMap Key 有效性检查改为复用动态搜索关键词，不再依赖模块启动时生成的静态查询常量。

## [1.7.18] - 2026-07-09

### 修复
- 修复扫描配置页"刷新余额"功能失效的问题：前端余额查询失败时静默无反馈，Key 列表停留在"加载中..."状态，用户无法判断是正在加载还是已失败。现在失败时会显示"余额查询失败"状态并弹出提示。
- 修复后端 `/api/scan/keys/credits` 接口缺少 timeout 导致某平台 API 响应慢时无限等待的问题，增加 50 秒超时保护。

### 改进
- 数据库连接增加重试机制：MySQL 连接失败时自动重试 5 次（间隔 3 秒），适配 Docker 启动时 MySQL 未就绪的场景；认证错误（1045）和数据库不存在（1049）不重试，直接报错。
- 应用启动不再因数据库暂时不可用而崩溃：`db.init_db()` 失败时记录错误日志并继续启动，等待首次请求时自动重试连接。
- 增强 Quake API 积分查询的健壮性：兼容 `month_remaining_credit` 字段可能缺失或改名的情况，增加 `month_remaining_data`、`remaining_credit` 等候选字段；兼容 `code` 为 `"0"` / `"200"` / `"success"` 多种格式；增加 DEBUG 原始响应日志和 HTTP 非 200 响应体日志。
- 增强 Hunter API 积分查询的健壮性：增加 `free_point`、`rest_point` 候选字段和 `daily_free_point` 兼容；增加非 200 响应体日志和超时日志。
- 增加 DayDayMap bffapi 接口的 DEBUG 状态码和原始响应日志。
- Fofa 平台显示"不支持余额查询"而非"余额未知"，明确区分设计限制与查询失败。
- DayDayMap 余额为 null 时前端显示"Key有效 (余额需登录查看)"而非"余额未知"。
- Quake API 超时从 10 秒提升到 15 秒，避免网络波动导致误报失败。

### 运维
- GitHub Actions 每次构建镜像后自动将项目描述（DOCKERHUB.md）和完整 CHANGELOG 拼接同步到 Docker Hub 仓库 Description，打开 Docker Hub 页面即可直接查看项目介绍和所有版本的更新日志。

## [1.7.17] - 2026-07-07

### 修复
- 修复扫描配置页"刷新余额"按钮报错的问题：`GET /api/scan/keys/credits` 接口缺少 `_ensure_scan_bridge()` 调用，导致 `scanner` 变量未定义（`NameError`），无法通过异步桥接查询各平台 Key 积分。

## [1.7.16] - 2026-07-06

### 修复
- 修复切换历史等标签页后，右上角“最近更新/耗时”执行提示可能回退到旧测速记录的问题；测速历史、最新记录和结果订阅统一按 `finished_at DESC, id DESC` 判断最新，前端接收历史列表时也会按完成时间选择最新记录。

## [1.7.15] - 2026-07-05

### 修复
- API Key 管理接口（`GET /api/scan/keys`、`GET /api/scan/keys/credits`）返回完整 key 值（而非仅后缀），修复编辑 Key 时因后缀匹配失败导致"原 Key 不存在"错误、无法保存的问题。
- 修复测试日志中混入其他模块（scheduler、database 等）日志的问题。`_DBLogHandler` 不再挂载到 root logger，改为使用专用的 `iptv_test_engine` logger，避免全局日志污染。
- 修复测试结束后前端持续轮询 `/progress` 接口、UI 假显示"运行中"的问题。测试完成后立即停止进度轮询；SSE 断线时仅在测试实际运行中才启动 HTTP 降级轮询；测试完成且调度器运行时自动重连 SSE 以等待下次调度。

### 改进
- 测试进度中增加数据源来源统计（订阅源数量、扫描源数量），日志输出 `数据源统计：订阅源 X 个，扫描源 Y 个，总计 Z 个`，API 返回 `sub_count` 和 `scan_count` 字段。

## [1.7.14] - 2026-07-04

### 修复
- 添加 `cryptography` 依赖，修复 Docker 中 pymysql 无法使用 `caching_sha2_password` 认证方式连接 MySQL 8 的问题。

## [1.7.13] - 2026-07-02

### 改进
- SSE 端点（`/api/scan/stream`、`/api/test/stream`、`/api/ip-scan/stream`）增加 30 分钟最大连接时长，防止异常客户端永久占用 Flask 工作线程。
- MySQL 数据库连接增加自动重连机制：捕获 `OperationalError`（MySQL 2006/2013/2014）时自动重建连接并重试一次。
- `asyncio.run()` 替换为 `scanner.bridge.run_sync()`，正确使用 AsyncBridge 处理异步积分查询。
- 前端构建 `subprocess.run()` 增加超时（npm install 120s，npm run build 180s），防止进程挂起。
- `_local_now()` 函数去重：4 个文件的重复定义统一到 `database.local_now()`。
- `requirements.txt` 锁定所有依赖版本（`==`），确保构建可复现。

### 运维
- 将 `.mimocode/` 目录添加到 `.gitignore`，避免工具生成的临时文件被提交。

## [1.7.12] - 2026-07-02

### 改进
- 融合扫描源参与测速不再过滤 `validated` 和 `quality_status`，池中所有未删除的扫描结果均参与测速。

### 修复
- 修复定时测速（`times` 模式）到了设定时间不触发测试的问题：调度器线程异常退出后文件锁未释放导致无法自动恢复，增加死亡线程清理、锁获取失败日志、陈旧数据库状态清理，以及 `times` 模式下等待测试完成后再计算下一个时间点。

## [1.7.11] - 2026-07-02

### 修复
- 修复历史明细展开行中折叠面板+内层表格的分页问题：翻页后所有已展开的频道面板被全部关闭（`expand-mutex` 与动态 `:key` 组合导致组件重建），改为 `v-model` 手动控制展开状态。
- 修复快速连续点击分页时可能出现页码与数据不一致的问题，给 `loadChannelPage` 添加请求序列号保护。
- 修复频道通过率显示 `NaN%` 的除零问题。
- 优化展开行模板中 `paginatedChannelInfo` 的重复调用，改为一次性提取频道信息减少渲染开销。

## [1.7.10] - 2026-06-30

### 修复
- 修复单同步 WSGI 或手动关闭 SSE 后，页面初始短轮询在空闲状态停止，点击“立即测试”不会重新拉起进度轮询，导致测速看起来异常卡住或无法看到日志的问题；短轮询启动时现在会立即同步一次状态。

## [1.7.9] - 2026-06-30

### 修复
- 修复检测概览点击“日志”打开明细弹窗时，如果本轮 `detection_results` 明细未落库会返回空内容的问题；明细写入改为分批落库，并在明细为空时按检测轮次时间窗口从持久化结果兜底返回频道检测状态。

## [1.7.8] - 2026-06-30

### 修复
- 修复扫描结果“按来源分组”表格多次点击排序取消时前端因空排序状态崩溃，导致表格消失的问题。
- 修复来源分组质量筛选在 MySQL 聚合计数字段以字符串返回时把 `"0"` 当作有效数量，导致下拉筛选看起来无效的问题。
- 修复手动触发持久化结果检测后，检测概览在 SSE 降级轮询或跨标签页场景下不能及时刷新，需要等待下一次轮询/手动查询才显示新轮次的问题。

## [1.7.7] - 2026-06-29

### 修复
- 修复定期检测成功检测单条记录后仍可能因质量评估入口缺失而中断整轮检测的问题，避免检测概览出现“检测数 0、通过 1、明细 0”的错位统计，并确保失败次数与阈值软删除流程继续执行。
- 定期检测单条结果后处理异常时改为按失败明细记录并继续本轮检测，异常或超时收尾时的检测数会按已处理结果兜底计算。

### 改进
- 扫描结果来源分组新增“最近检测”和“最近入库”两个时间口径，并保留原 `last_updated` 字段兼容旧调用，避免把单个频道检测时间误读为整组更新时间。

## [1.7.6] - 2026-06-29

### 修复
- 修复部署包 `deploy/database/__init__.py` 未同步导出 `_evaluate_quality`、软删除复活和 IP 扫描相关数据库函数，导致使用 deploy 包运行时频道扫描或检测流程仍可能报 `module 'database' has no attribute ...` 的问题。
- 扫描收益统计的质量分档增加数据库入口兜底导入，避免数据库包入口漏导出时直接中断整轮扫描。

### 改进
- 优化频道扫描页失败态展示：失败原因改为独立告警块展示，扫描概览只保留时间和阶段统计，避免错误徽标和长错误文案在深色界面中显得突兀。
- 日志面板与扫描统计卡片改为使用 TDesign 主题变量，减少深色主题下亮色渐变卡片和大面积紫色日志背景造成的视觉割裂。

## [1.7.5] - 2026-06-29

### 修复
- 修复 Docker 部署中 `database` 包入口未导出质量评估、频道复活和 IP 扫描数据库函数，导致频道扫描、检测复活或 IP 扫描相关接口返回 `module 'database' has no attribute ...` 的问题。
- 前端新增运行时 SSE 能力自动判断：多线程 WSGI 环境自动使用 SSE，旧 Compose 或面板仍覆盖为单同步 worker 时自动降级短轮询，避免长连接占满唯一 worker。
- 新增 `/api/ip-scan/logs` 增量日志接口，让 IP 扫描在短轮询模式下仍能刷新日志。

### 运维
- 同步 `deploy/Dockerfile` 的 Gunicorn 默认启动参数为 `gthread` 多线程模式，避免使用部署产物构建镜像时仍保留旧的单同步 worker 配置。
- Docker Compose 部署文件显式写入应用容器的 `gthread` 启动命令，避免面板重部署时把 CMD 恢复为旧的单同步 worker 参数。

## [1.7.4] - 2026-06-29

### 修复
- 修复 Docker/Gunicorn 单同步 worker 被 SSE 长连接占用后，首页、异步标签页 JS 分包和 API 请求持续挂起，导致切换标签页空白的问题；Docker 默认改为单进程多线程 `gthread` 模式。

## [1.7.3] - 2026-06-29

### 修复
- 修复生产构建中切换标签页时，标签内容可能未被标记为已访问而不挂载组件，导致页面空白且没有发起对应接口请求的问题。

## [1.7.2] - 2026-06-28

### 修复
- 修复 Docker 使用旧 MySQL 数据卷升级后，部分新增字段未自动补齐导致检测、扫描、持久化结果或 IP 扫描相关接口返回 500，并引发前端轮询请求持续报错的问题。

## [1.7.1] - 2026-06-28

### 修复
- 修复系统配置、配置导入、文本文件保存和检测配置保存对已解包 API 响应的误判，避免成功保存后仍提示失败。
- 修复扫描结果来源详情弹窗标题不显示、详情筛选只影响当前页且不重置分页的问题；详情接口新增搜索、质量、分类和省份服务端过滤。
- 修复 Fofa Key 新增/编辑时未保存 Email 导致 Fofa 扫描被跳过的问题，编辑 Key 时会回填并同步 `fofa_email`。
- 修复 `Ctrl+F` 快捷键调用 `preventDefault()` 报错，以及隐藏页签恢复后轮询不再继续 tick 的问题。
- 修复趋势图在窄容器下 X 轴标签重叠的问题。

### 改进
- 扫描详情表格和 API Key 表格补充横向滚动适配，移动端不再因列宽不足看起来缺少关键列。
- 扫描结果旧版“按扫描记录”入口改名为“历史模式”，保留功能但降低主流程冗余感。
- Tab 页面改为异步导入，并配置 Vite vendor manual chunks；移除不再使用的 `@vueuse/core` 依赖。
- 将当前未消费的频道发现和多方案前端 API 封装标记为清理候选，后端兼容路由本轮不删除。

### 安全
- 从 Git 索引移除 `basic_auth.json`，保留 `basic_auth.json.example` 和 ignore 规则覆盖；已暴露 BasicAuth 凭据需要在部署环境轮换。

## [1.7.0] - 2026-06-28

### 新增
- 新增测绘平台/画像收益统计表 `scan_yield_stats`，记录每轮扫描中平台、画像、省份维度的目标量、API 命中、实际探测、频道提取、清洗、去重、候选、快筛通过、深度检测通过和质量分档数量。
- 新增 `/api/scan/yield-stats` 接口和前端 API 封装，用于查询已持久化的平台/画像扫描收益数据。
- 扫描配置新增 `deep_check_duration`、`deep_check_min_bytes`、`deep_check_request_timeout`，用于统一控制扫描板块深度检测采样时长、最小采样字节数和请求超时。
- 扫描配置页新增深测时长、深测采样和深测超时控件，便于直接调整扫描板块统一深度检测口径。

### 改进
- 测绘平台基础查询与质量优先画像查询会为候选频道打上内部收益标识，扫描主流程在快筛和深度检测结束后回填真实产出，便于后续按平台和画像动态调整额度。
- 扫描后的后台深度检测、持久化结果手动重检和定期检测统一通过同一个 `run_deep_check()` 入口执行，避免扫描板块内部检测口径分散。
- 删除或清理扫描历史时同步清理对应的平台/画像收益统计记录，避免历史数据残留。

## [1.6.17] - 2026-06-28

### 改进
- 检测概览新增轮次状态标识，异常中断的历史轮次会显示为异常，鼠标悬停可查看原因。

### 修复
- 修复定期检测轮次在超时、取消或异常中断时只留下 `0` 汇总的问题；现在会尽量写入已完成明细、耗时和错误原因，并避免调度循环被单次异常打断。
- 修复检测概览日期范围查询结束日只包含当天 `00:00:00` 的问题，日期选择到某天时会包含该日完整数据。

## [1.6.16] - 2026-06-28

### 改进
- README 按当前项目实现重新校准描述，区分 Docker `.env` 与源码直跑 `database/db_config.json` 的数据库配置入口。
- README 修正 `/api/health` 默认轻量响应和 `IPTV_HEALTH_DETAILED=1` 详细响应示例，移除旧版 `ok/data` 与 `1.6.3` 示例。
- README 修正 Gunicorn 端口说明、API 响应格式例外、结果订阅端点、输出文件示例、Web 标签页和项目结构说明。
- README 移除 Webhook 作为后台可直接启用功能的描述，改为说明当前仅保留通知发送工具代码，默认配置链路尚未开放。

## [1.6.15] - 2026-06-27

### 安全
- 移除 Docker Compose 默认 MySQL root 弱密码，`DB_PASSWORD` 未设置时会拒绝启动，避免误用固定默认密码。
- 新增 `generate_env.py`，可自动生成带随机 `DB_PASSWORD` 的 `.env`，并避免默认覆盖已有数据库密码。

### 运维
- `.env.example` 改为留空 `DB_PASSWORD` 并提示生成随机密码，部署打包脚本同步带上 `.env.example` 和 `generate_env.py`。
- README 同步更新随机密码生成流程、飞牛 Compose 示例和当前版本号。
- README 新增 GitHub 首页可见的最近更新摘要，并补充默认 MySQL Docker volume 持久化、备份、恢复和 `docker compose down -v` 风险说明。

## [1.6.14] - 2026-06-27

### 运维
- 默认 Docker Compose 内置 MySQL 新增宿主机本地端口映射，仅监听 `127.0.0.1:${MYSQL_HOST_PORT:-3306}`，便于本机数据库客户端访问且不暴露到局域网或公网。
- `.env.example` 新增 `MYSQL_HOST_PORT`，可在宿主机 3306 被占用时调整本地映射端口。
- README 同步更新 MySQL 本地访问说明、飞牛 Compose 示例和当前版本号。

## [1.6.13] - 2026-06-27

### 运维
- 默认 Docker Compose 内置 MySQL 容器名从 `iptv-mysql` 调整为 `mysql`，与服务名保持一致，避免额外的项目名前缀。
- README 同步更新默认 MySQL 容器命名、飞牛 Compose 示例和当前版本号。

## [1.6.12] - 2026-06-27

### 运维
- 默认 Docker Compose 内置 MySQL 启用轻量启动参数，关闭 Performance Schema，并收紧 InnoDB 缓冲池、日志缓冲、连接数和表缓存，降低小内存服务器上的基础内存占用。
- `.env.example` 新增默认 MySQL 容器内存相关调优项，用户可按服务器规格覆盖缓冲池、连接数和缓存大小。
- README 同步更新默认 MySQL 低内存配置说明、飞牛 Compose 示例和当前版本号。

## [1.6.11] - 2026-06-27

### 修复
- 快速过滤通过但尚未深度测速的频道不再用空字符串填充 `delay`、`bandwidth`，避免 MySQL 严格模式把 DOUBLE 字段写入判为异常。
- 统一测速结果、检测结果、质量历史和 IP 扫描响应时间等 DOUBLE 字段的写库归一化，空值、异常字符串和非有限数会写入 `NULL` 或 `0`。
- 修复单条持久化检测写入质量历史时重复获取数据库写锁，可能导致检测流程卡住的问题。

## [1.6.10] - 2026-06-26

### 改进
- 省积分模式仅在未手动指定扫描平台时收缩到 Quake；关闭省积分模式或手动指定平台时，质量优先查询会跟随所有启用渠道执行。
- 扫描配置页文案补充 Fofa，并将关闭省积分模式说明调整为适合每日免费额度充足的多渠道扫描场景。

### 运维
- Docker Compose 默认 MySQL 镜像从 `mysql:8` 收窄为 `mysql:8.4`，明确使用 MySQL 8.4 LTS 版本线，避免浮动标签带来的部署版本不确定。
- README 同步更新默认 MySQL 容器说明和当前版本号。

## [1.6.9] - 2026-06-26

### 新增
- 质量优先查询新增 `txiptv_live` 画像，专门搜索 `/tsfile/live/` 与 `key=txiptv` 组合以及 `1000.json?key=txiptv` 入口。

### 改进
- 根据最近一次测速结果收缩默认扫描策略：默认画像调整为 `txiptv_live`、`live_interface`、`zhgx`、`tvheadend`，默认画像预算从 240 降为 120。
- 基础搜索关键词移除 JSMpeg、频道 API 等低优先级入口，优先保留能产出高质量 `/tsfile/live/*.m3u8?key=txiptv` 源的特征。
- 省积分模式下默认跳过独立 JSMpeg 扫描，避免在最新测速未证明高收益的画像上继续消耗搜索积分。

## [1.6.8] - 2026-06-26

### 改进
- 频道扫描新增“省积分模式”，未手动指定平台时优先使用 Quake，并跳过隐藏的 Hunter/Tvheadend/IPTV互动等低收益补扫。
- 基础搜索关键词收敛为直播 JSON、ZHGX、JSMpeg、频道 API 等直接可提取入口，移除泛品牌、泛标题、M3U、运营商播放列表和 Xtream 等默认 OR 关键词。
- 质量优先查询默认仅使用 Quake 平台，并默认移除最近日志低收益或噪声偏高的 M3U 播放列表、运营商播放列表、中间件品牌、Xtream 画像，减少无产出的 API 查询。
- 频道扫描页优化按钮状态与文案：“开始扫描”会随启动/运行状态变化，启动后同时启用状态轮询兜底，“强制清除状态”改为“重置卡死状态”并补充适用场景。

### 修复
- 修复扫描快速过滤通过后，`delay`、`bandwidth` 等 DOUBLE 字段写入空字符串导致 MySQL 报 `Incorrect DOUBLE value` 并把扫描标记为失败的问题。
- 最近扫描失败时，频道扫描页会在进度与概览中展示失败原因，避免只看到空闲或重启重置状态。

## [1.6.7] - 2026-06-26

### 改进
- 优化扫描结果页“按扫描记录”视图：首次进入默认选中最近一轮扫描，筛选栏新增“最新一轮”“全部记录”快捷切换、加载状态和结果数量反馈。

### 修复
- 修复扫描记录下拉未兼容 `/api/scan/history` 返回的 `items` 字段，导致只能看到“全部扫描记录”的问题。

## [1.6.6] - 2026-06-26

### 改进
- 优化 `/api/initial` 首屏数据接口：默认仅返回总览所需摘要数据，不再重复返回最新测速明细和频道聚合明细，将当前数据集下的首包响应从约 3.29 MB 降至约 15 KB。
- 保留 `/api/initial?include_details=1` 兼容开关，用于需要旧版完整明细负载的调用场景。

## [1.6.5] - 2026-06-26

### 新增
- 扫描模块新增质量优先查询：在 Quake、Hunter、DayDayMap、Fofa 基础查询之外，追加标准直播接口、ZHGXTV、JSMpeg、频道 API、M3U、组播代理、Tvheadend、运营商播放列表等高价值画像查询。
- 新增质量热点补源：根据历史频道的稳定性、延迟、带宽和质量状态识别高价值 /24 网段，并围绕稳定源继续探测。
- 扫描配置页新增质量优先查询、画像查询预算、质量热点补源、热点探测预算、最低稳定性等高级扫描策略配置项。
- 新增 `web/routes/params.py`，统一路由整数参数的默认值、上下限和异常输入处理。

### 改进
- API 平台采集日志改为展示目标量、API 命中、实际探测、频道提取和 C 段补充数量，便于判断扫描结果偏少的具体环节。
- 基础搜索与质量优先查询会先整体包裹 OR 查询，再追加省份和运营商过滤，避免过滤条件只作用到最后一个关键词。
- 基础搜索关键词改为结构化生成，补充 `/channels`、`channel_list.json`、`/api/live/channels`、`/live/channels.json`、`/playlist?profile=pass`、`udpxy/udp/rtp chanlist`、咪咕和 ICNTV 播放列表等可被提取器识别的入口。
- 质量优先查询继承已选省份配置，并按省份和画像拆分 `quality_query_profile_size` 预算。
- `quality_query_profile_size` 默认值从 120 调整为 240，以适配更多画像和多省份扫描。
- Hunter 支持独立的 `hunter_size` 目标量配置，不再复用 Quake 的扫描数量。
- 扩展平台解析与提取缓存，减少重复抓取并提升 IPTV 接口、频道 API、播放地址的提取覆盖。
- `/api/health` 默认返回轻量健康信息，可通过 `IPTV_HEALTH_DETAILED=1` 开启磁盘、内存和调度等详细检查。
- Docker/Gunicorn 默认使用单 worker，避免后台扫描、SSE 和停止信号在多进程下状态不一致。
- README 补充质量优先发现、质量热点补源、扫描偏少排查和新增扫描参数说明。
- README 完善 Docker 部署教程，明确默认 MySQL 容器、外部 MySQL 和端口映射的使用方式。
- README 补充说明 `docker pull` 只下载镜像，不会启动应用或 MySQL，避免误解部署命令。
- README 新增飞牛 fnOS 图形界面部署说明，提供可直接粘贴的 Compose 配置。
- 新增 `docker-compose.external-mysql.yml`，支持只启动应用容器并连接用户已有的 MySQL。

### 修复
- 修复 MySQL 索引创建在部分版本或重复迁移时可能失败的问题。
- 修复扫描、历史、检测和 IP 扫描接口中分页、limit、rate limit 等参数缺少边界保护的问题。
- 修复前端扫描结果、历史详情和 IP 扫描页面在异常参数或长轮询场景下的刷新与状态处理问题。
- 修复 Vite 开发代理读取认证配置时未优先使用环境变量的问题。

### 安全
- BasicAuth 在缺少配置文件且未设置密码时改为生成随机密码，避免继续使用固定默认密码。
- 对多处 API 查询参数增加类型转换和上限钳制，降低异常输入导致的资源消耗风险。

## [1.6.4] - 2026-06-26

### 新增
- 支持通过环境变量配置数据库连接（DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME）
- Docker Compose 内置 MySQL 8，开箱即用
- 添加 `.env.example` 配置示例文件

### 改进
- 优化 Docker 部署文档，支持一键启动

## [1.6.3] - 2026-06-25

### 修复
- 修复历史明细选项卡首次进入时数据闪烁问题：移除 `initialRuns` 初始数据，改为加载时显示 loading 状态

## [1.6.2] - 2026-06-23

### 修复
- 修复 `scanner_integration/__init__.py` 第430行和第868行注释与代码粘连导致的逻辑错误
- 修复 `web/routes/health.py` 中引用不存在的 `_bridge` 变量，改为正确的 `bridge`
- 修复 `scanner_integration/persistence.py`、`web/routes/history.py`、`web/routes/scan.py`、`web/routes/subscribe.py` 中SQL占位符从SQLite风格 `?` 改为MySQL兼容的 `%s`
- 实现 `scanner_integration/video_check.py` 中缺失的 `health_check_persistent` 函数
- 修复 `scanner_integration/__init__.py` 中引用不存在的 `_db.DB_PATH` 属性，适配MySQL架构

### 改进
- 重构 `scanner_integration/__init__.py` 中重复的去重代码，提取 `_deduplicate_and_normalize()` 辅助函数
- 优化 `Dockerfile` 使用多阶段构建，减小镜像体积
- 将 `Dockerfile` 中的Flask内置服务器替换为gunicorn生产服务器
- 修复 `docker-compose.yml` 中已弃用的 `version` 字段
- 修复 `.gitignore` 中重复的 `basic_auth.json` 条目
- 优化 `frontend/vite.config.js` 添加try-catch处理配置文件缺失情况
- 清理 `tests/` 目录中的测试报告JSON和截图文件
- 创建 `.dockerignore` 文件优化Docker构建
- 在 `requirements.txt` 中添加 `gunicorn>=21.2.0` 依赖

### 安全
- 将 `database/db_config.json` 添加到 `.gitignore` 忽略列表

### 运维
- 更新 `docker-compose.yml` 添加 `db_config.json` volume 挂载，支持MySQL数据库配置
- 更新 README.md 版本号至 v1.6.2

## [1.6.1] - 2026-06-21

### 修复
- 修复 `scanner_integration/__init__.py` 文件编码损坏问题（109 个 U+FFFD 替换字符）
- 修复 30+ 个 docstring 缺失第三个闭合引号导致的 SyntaxError
- 修复 12 个字符串缺失闭合引号的问题
- 修复多处注释与代码同行导致的缩进错误
- 修复 `database/db.py` 中旧数据库升级时 `deleted_at` 列不存在导致 `CREATE INDEX` 失败的问题
- 添加 `_table_exists()` 辅助函数，将列补齐逻辑移到 `executescript()` 之前执行

## [1.6.0] - 2026-06-21

### 新增
- 键盘快捷键支持：Ctrl+S保存、Ctrl+F搜索、Alt+1-8切换Tab
- 无障碍访问(a11y)支持：语义化标签、ARIA属性、焦点管理
- 前端公共组件：LogPanel日志面板、useClipboard剪贴板、平台/质量/日期工具函数
- 健康检查增强：FFmpeg/磁盘/内存/扫描模块检查，版本和运行时间信息
- 表单即时验证反馈
- 操作确认弹窗（终止测试、停止扫描、强制清除、清空日志）

### 改进
- API响应格式统一为 `{'ok': True/False, 'data': ...}` 格式
- 全局错误处理器：404/500/405返回JSON而非HTML
- 分页参数添加上限校验（最大200条）
- 前端轮询优化：移除TestingTab双重轮询，添加指数退避和Tab可见性感知
- 数据库索引优化：添加复合索引、唯一约束、scan_logs.level字段

### 修复
- 修复API端点缺少HTTP方法限制的问题
- 修复ScannerTab triggerPending吞掉错误的问题

## [1.5.0] - 2026-06-21

### 新增
- 集成 Quake/Hunter/DayDayMap 扫描模块，自动发现酒店 IPTV 服务器
- 支持 ZHGXTV、JSMpeg、Tvheadend、IPTV互动等多种 IPTV 系统扫描
- C 段扫描：对已发现的 IP 所在 /24 子网进行扩展扫描
- 省份/城市/运营商过滤功能
- 健康检查：定时检测已发现频道的可用性，自动移除失效频道
- DuckDuckGo 扫描支持（可选）
- Fofa 平台扫描支持

### 改进
- 扫描结果可选择性送入测速流水线
- 自动频道名归一和分类（央视频道、卫视频道、各省频道）
- 快速过滤（HEAD 请求验证）+ 深度检测（带宽/延迟/稳定性/分辨率）

## [1.4.0] - 2026-06-20

### 新增
- SQLite 数据库存储，替代原有 JSON 文件存储
- 数据库自动迁移：首次启动自动迁移 config.json 和 history.json
- 测速历史、详情、日志和实时进度保存在 SQLite 中
- 扫描历史和结果持久化存储

### 改进
- 数据库最多保留最近 50 轮历史记录
- 日志保留策略：测速日志 30 天、扫描日志 7 天、持久化结果 90 天
- 支持通过环境变量 `IPTV_DB_PATH` 自定义数据库路径

## [1.3.0] - 2026-06-19

### 新增
- Flask Web 后台，提供完整的 API 接口
- Vue 3 + TDesign 前端界面
- 支持深色/浅色主题切换
- 总览、历史明细、系统测试、系统配置、频道扫描、扫描配置、检测监控、扫描结果等标签页
- BasicAuth 认证保护
- Vite 开发模式，支持前端热更新

### 改进
- 重构目录结构，分离前后端代码
- 支持 Docker 部署（Dockerfile + docker-compose.yml）
- 支持 Gunicorn 生产部署

## [1.2.0] - 2026-06-18

### 新增
- FFmpeg 分辨率检测和编码信息获取
- 带宽测速：结合下载采样计算带宽
- 支持 H.265/HEVC 编码带宽折算
- 频道别名归一系统（精确匹配 + 正则匹配）
- 模板化输出：按频道模板只测试需要的频道

### 改进
- 实时写入输出文件（TXT 和 M3U 格式）
- 支持最低分辨率、最低带宽筛选
- 单频道输出数量限制

## [1.1.0] - 2026-06-17

### 新增
- 多 M3U 订阅源聚合解析
- 频道模板系统，支持分类和频道名
- 别名映射，支持精确别名和正则别名
- 运行模式：once、times、interval
- 测速结果输出到 TXT 和 M3U 文件

### 改进
- 优化测速并发控制
- 支持系统带宽和内存限制

## [1.0.0] - 2026-06-16

### 新增
- 基础测速功能
- 命令行运行模式
- 配置文件支持（config.json、subscribe.txt、alias.txt、demo.txt）
- 测速历史记录（output/history.json）

### 说明
- 初始版本，实现核心测速引擎
- 支持基本的频道匹配和测速逻辑
